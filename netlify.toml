[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Serve the booth at the site root (kiosk just opens https://<site>/ ).
[[redirects]]
  from = "/"
  to = "/anf_photobooth.html"
  status = 200
