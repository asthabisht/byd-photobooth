[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Serve the booth at the site root (so the kiosk just opens https://<site>/ ).
[[redirects]]
  from = "/"
  to = "/byd_ti7_photobooth.html"
  status = 200
